# Deploy on Vercel

This project uses TanStack Start with the Nitro Vite plugin. Vercel detects TanStack Start + Nitro automatically.

Use the repository root as the Root Directory. Do not override the Framework Preset, Build Command, or Output Directory.

## Local check

```bash
npm install
npm run build
npm run preview
```

No environment variables are required for this portfolio.
